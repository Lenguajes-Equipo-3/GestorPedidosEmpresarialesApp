<Window x:Class="GestorPedidosEmpresarialesApp.Views.ProductoBaseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GestorPedidosEmpresarialesApp.ViewModel"
        xmlns:local="clr-namespace:GestorPedidosEmpresarialesApp.Converters"
        mc:Ignorable="d"
        Title="Gestión de Productos Base" Height="450" Width="800">

    <Window.Resources>
        <local:NullToBoolConverter x:Key="NullToBoolConverter" />
    </Window.Resources>

    <Window.DataContext>
        <vm:ProductoBaseViewModel />
    </Window.DataContext>

    <Grid>
        <DockPanel>
            <DataGrid ItemsSource="{Binding ProductosBase}" SelectedItem="{Binding ProductoSeleccionado}" AutoGenerateColumns="False" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdProductoBase}" Width="50" />
                    <DataGridTextColumn Header="Nombre" Binding="{Binding NombreProducto}" Width="200" />
                    <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria.Descripcion}" Width="150" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>