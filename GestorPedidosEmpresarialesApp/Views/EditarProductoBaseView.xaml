<Window x:Class="GestorPedidosEmpresarialesApp.Views.EditarProductoBaseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Producto" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Nombre:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" />
        <TextBox Text="{Binding Producto.NombreProducto, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" />

        <TextBlock Text="Categoría:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,10,0" />
        <ComboBox ItemsSource="{Binding Categorias}" SelectedItem="{Binding Producto.Categoria}" DisplayMemberPath="Descripcion" Grid.Row="1" Grid.Column="1" Margin="0,10,0,0" />

        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Guardar" Command="{Binding GuardarCambiosCommand}" Width="75" Margin="0,0,10,0" />
            
        </StackPanel>
    </Grid>
</Window>